# PDFサイネージシステム 開発計画

要件定義書（`アプリ要件定義書.md`）に基づき、依存関係が少なく各フェーズで「動くものが1つ増える」形に分解した開発計画。

早期フェーズは設定をコード／JSON 直書きで進め、管理画面は後半でまとめて接続する想定。

---

## 全体像

```mermaid
flowchart LR
  P0[Phase 0\n基盤] --> P1[Phase 1\n静止画]
  P1 --> P2[Phase 2\nPDF]
  P2 --> P3[Phase 3\n動画]
  P3 --> P4[Phase 4\nリスト・時間]
  P4 --> P5[Phase 5\n監視]
  P5 --> P6[Phase 6\nキオスク]
  P6 --> P7[Phase 7\n管理画面]
  P7 --> P8[Phase 8\nエラー・復旧]
  P8 --> P9[Phase 9\nスケジュール]
  P9 --> P10[Phase 10\nリリース]
```

| Phase | 名称 | この段階で「できること」 |
|-------|------|------------------------|
| 0 | 基盤構築 | プロジェクト起動、設定の読み書き |
| 1 | 静止画サイネージ | JPEG を全画面でループ表示 |
| 2 | PDF 表示 | 複数ページ PDF を順送り |
| 3 | 動画表示 | MP4 を最後まで再生 |
| 4 | リスト・時間制御 | 混在コンテンツを自然順・個別秒数でループ |
| 5 | フォルダ監視 | ファイル追加・削除をスライド後に反映 |
| 6 | キオスク強化 | タスクバー非表示・Alt+Tab 抑制 |
| 7 | 管理画面 | 全設定を GUI で変更 |
| 8 | エラー・復旧 | ログ・スキップ・自己再起動・障害メッセージ |
| 9 | スケジュール・自動起動 | 終了時刻・shutdown・Windows 起動時起動 |
| 10 | リリース準備 | ポータブル exe・性能確認・受け入れテスト |

---

## Phase 0: 基盤構築

**目的:** 以降の機能を載せる土台を作る。

| # | タスク | 詳細 |
|---|--------|------|
| 0-1 | WPF プロジェクト作成 | .NET 8（Windows 11 向け）、ターゲット `win-x64` |
| 0-2 | フォルダ構成 | `Models` / `Services` / `ViewModels` / `Views` 等 |
| 0-3 | 設定モデル定義 | 監視フォルダ、表示秒数、各時刻、障害メッセージ等 |
| 0-4 | 設定の永続化 | exe 横の `settings.json` 読み書き |
| 0-5 | ログ基盤 | `logs` パス算出（監視フォルダの親階層 + `\logs`） |
| 0-6 | 開発用監視フォルダ | 本番は `D:\Signage`、開発はプロジェクト内 `SignageData` |

**完了条件:** アプリ起動 → 設定読込 → ログフォルダパスが正しく算出される。

---

## Phase 1: 静止画サイネージ（最初のデモ）

**目的:** 最小の「サイネージ」を動かす。PDF・動画はまだ入れない。

| # | タスク | 詳細 |
|---|--------|------|
| 1-1 | メインウィンドウ全画面 | `WindowState=Maximized`、背景黒 |
| 1-2 | フォルダスキャン | `.jpg` / `.jpeg` を直下のみ取得 |
| 1-3 | 自然順ソート | `001`, `002`, `010` が正しい順になること |
| 1-4 | 画像表示 | 中央配置・アスペクト比維持・余白あり |
| 1-5 | タイマー切替 | デフォルト 15 秒（設定値）で次画像へ |
| 1-6 | ループ | 最後の画像の次は先頭へ |

**完了条件:** フォルダに JPEG を置くと全画面で無限ループ表示される。

**意図的に後回し:** キオスク完全化、管理画面、ファイル名 `_秒数`。

---

## Phase 2: PDF 表示

**目的:** 静止画に PDF（複数ページ）を追加。

| # | タスク | 詳細 |
|---|--------|------|
| 2-1 | PDF ライブラリ選定・導入 | 例: Pdfium / Docnet 等（WPF 描画方式を決定） |
| 2-2 | 1 ページ表示 | 画面フィット（余白あり） |
| 2-3 | 複数ページ順送り | 1 ページ = 1 スライドとして扱う |
| 2-4 | ページ切替タイマー | 各ページにデフォルト表示秒数を適用 |
| 2-5 | PDF 全ページ後 | 次のファイルへ進む |

**完了条件:** 3 ページ PDF が 3 スライドとして表示され、JPEG と混在してもループする。

---

## Phase 3: 動画表示

**目的:** MP4 を「最後まで再生してから次へ」を実現。

| # | タスク | 詳細 |
|---|--------|------|
| 3-1 | MediaElement 等で MP4 再生 | 全画面・中央配置 |
| 3-2 | 再生完了検知 | `MediaEnded` で次スライドへ（秒数打ち切りなし） |
| 3-3 | 画像・PDF との切替 | 動画前後でリソース解放（メモリリーク防止） |
| 3-4 | 音声 | 要件未記載 → **デフォルトは音声あり**（後で設定化可） |

**完了条件:** `画像 → PDF → MP4 → 画像` のループが途切れない。

---

## Phase 4: 表示リスト・時間制御の統合

**目的:** スライドショー全体を 1 本のオーケストレーターにまとめる。

| # | タスク | 詳細 |
|---|--------|------|
| 4-1 | `Slide` モデル | ファイル種別・パス・ページ番号・表示秒数 |
| 4-2 | プレイリスト生成 | スキャン → PDF ページ展開 → スライド列 |
| 4-3 | ファイル名 `_秒数` 解析 | `003_案内_120.pdf` → 120 秒（拡張子直前の `_数字`） |
| 4-4 | 動画は秒数無視 | 動画スライドは再生完了のみで進行 |
| 4-5 | 切替性能 | 次スライド表示まで 3 秒以内を目標に計測 |
| 4-6 | 空フォルダ時 | 黒画面 or 障害メッセージ（方針をここで決定） |

**完了条件:** 要件の表示順例（PDF 複数ページ・`_120`・MP4 完走）が一通り動く。

---

## Phase 5: フォルダ監視

**目的:** ファイルを置くだけで更新（スライド完了後に反映）。

| # | タスク | 詳細 |
|---|--------|------|
| 5-1 | `FileSystemWatcher` 実装 | 追加・削除・更新を検知 |
| 5-2 | 遅延反映 | 変更フラグを立て、**現在スライド完了後**にリスト再構築 |
| 5-3 | 表示中インデックス維持 | 可能なら現在ファイルを継続、削除時は次へ |
| 5-4 | サブフォルダ除外 | 直下のみ |
| 5-5 | 更新中ファイル | コピー途中の読込失敗は Phase 8 でログ＋スキップ |

**完了条件:** 表示中にファイル追加・削除しても、現在スライド後にリストが更新される。

---

## Phase 6: キオスクモード強化

**目的:** 通常モードをキオスク相当にする（管理画面はまだ最小でよい）。

| # | タスク | 詳細 |
|---|--------|------|
| 6-1 | 枠なし全画面 | `WindowStyle=None`、`ResizeMode=NoResize` |
| 6-2 | タスクバー非表示 | Win32 `FindWindow` + `ShowWindow` 等 |
| 6-3 | Alt+Tab 抑制 | 可能な範囲（完全抑制は OS 制約あり → 文書化） |
| 6-4 | `Ctrl+Shift+M` フック | 管理モード切替の土台（画面は Phase 7） |
| 6-5 | シングルディスプレイ | プライマリ画面に固定 |

**完了条件:** 起動直後からキオスク表示。`Ctrl+Shift+M` で何らかの画面切替ができる。

---

## Phase 7: 管理画面（全設定 GUI）

**目的:** 要件 4.1 の設定項目をすべて GUI で操作可能にする。

| # | タスク | 詳細 |
|---|--------|------|
| 7-1 | 管理画面 UI | 別 Window または同一 Window の切替 |
| 7-2 | 監視フォルダ | パス入力 + フォルダ選択 |
| 7-3 | デフォルト表示秒数 | 5〜300 秒バリデーション |
| 7-4 | Windows 自動起動 | ON/OFF（レジストリは Phase 9 で実装、ここは UI 連携） |
| 7-5 | アプリ終了時刻 | 時刻ピッカー |
| 7-6 | PC 電源オフ時刻 | 時刻ピッカー |
| 7-7 | 復帰不能時メッセージ | テキスト入力 |
| 7-8 | バージョン情報 | アプリバージョン表示 |
| 7-9 | 保存・適用 | `settings.json` 保存、実行中サービスへ反映 |
| 7-10 | 通常モードへ復帰 | キオスク表示に戻るボタン |

**完了条件:** 管理画面だけで全設定を変更し、再起動なしで（可能な範囲で）反映される。

---

## Phase 8: エラー処理・障害対策

**目的:** 壊れたファイルでも止まらず、ログと復旧 UI を整備。

| # | タスク | 詳細 |
|---|--------|------|
| 8-1 | ファイル読込エラー | try/catch → ログ → スキップ → 次へ |
| 8-2 | エラーログ出力 | `D:\logs`（監視フォルダ連動）に日次ログ |
| 8-3 | 復帰不能画面 | 設定メッセージを全画面表示（例: 「表示を復旧しています」） |
| 8-4 | 連続失敗検知 | 全ファイル失敗・起動不能時に復帰不能画面へ |
| 8-5 | 自己再起動 | 未処理例外時にプロセス再起動（二重起動防止） |
| 8-6 | 監視フォルダ変更時 | ログ出力先パスも連動更新 |

**完了条件:** 破損 PDF を混ぜてもループ継続。ログに記録。意図的クラッシュで再起動する。

---

## Phase 9: スケジュール・Windows 自動起動

**目的:** 夜間停止・朝の自動運用を実現。

| # | タスク | 詳細 |
|---|--------|------|
| 9-1 | 時刻スケジューラ | 日次のアプリ終了・電源オフを監視 |
| 9-2 | アプリ終了 | 指定時刻に正常終了 |
| 9-3 | PC 電源オフ | `shutdown /s /t 0`（管理者権限要否を確認・文書化） |
| 9-4 | Windows 自動起動 | レジストリ `Run` キー or スタートアップフォルダ |
| 9-5 | 管理画面連携 | Phase 7 の ON/OFF・時刻設定と接続 |
| 9-6 | 翌朝の流れ | PC 起動 → ログオン → アプリ自動起動 → 表示開始 |

**完了条件:** 設定時刻でアプリ終了・PC オフ。再起動後に自動でサイネージ開始。

**注意:** `shutdown` は権限・ポリシーにより失敗する可能性があるため、ログ出力と管理画面での注意表示を入れる。

---

## Phase 10: リリース準備・受け入れ

**目的:** MVP として配布可能な品質にする。

| # | タスク | 詳細 |
|---|--------|------|
| 10-1 | ポータブル publish | `dotnet publish -r win-x64 --self-contained` 等 |
| 10-2 | 性能測定 | 起動 30 秒以内、切替 3 秒以内、メモリ 500MB 以下 |
| 10-3 | 長時間運転テスト | 24h ループ・メモリ増加確認 |
| 10-4 | 受け入れテスト | 要件定義書 9 章のチェックリスト化 |
| 10-5 | 運用ドキュメント | フォルダ運用・管理キー・ログ場所・権限要件 |
| 10-6 | 既知の制限事項 | Alt+Tab 完全抑制不可、shutdown 権限等 |

**完了条件:** ポータブル exe 1 セットで現場投入可能。

---

## フェーズ間の依存と進め方のコツ

1. **Phase 1〜3 はメディア種別ごとに分離** — 一度に PDF+動画を入れない。
2. **管理画面は Phase 7** — それ以前は `settings.json` または定数で開発。
3. **キオスクは Phase 6** — まず表示ロジックを固めてから UI 制限を足す。
4. **各 Phase 末に手動テスト** — 上記「完了条件」を満たしてから次へ。

---

## 推奨スプリント（目安）

| スプリント | Phase | おおよその内容 |
|-----------|-------|----------------|
| S1 | 0 + 1 | 基盤 + 静止画デモ |
| S2 | 2 + 3 | PDF + 動画 |
| S3 | 4 + 5 | 統合 + 監視 |
| S4 | 6 + 7 | キオスク + 管理画面 |
| S5 | 8 + 9 | 障害対策 + スケジュール |
| S6 | 10 | リリース |

---

## 次のアクション

**Phase 9（スケジュール・自動起動）** から着手する。時刻スケジューラと Windows 自動起動の実装から開始。
