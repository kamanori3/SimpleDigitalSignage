# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に沿って記述し、バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従う。

バージョンは `PdfSignage/PdfSignage.csproj` の `<Version>` が正で、管理画面のタイトルバーに表示される。**リリース時は csproj のバージョンとこのファイルを必ず同時に更新すること。**

分類は `追加` / `変更` / `非推奨` / `削除` / `修正` / `セキュリティ` を使う。

## [未リリース]

### 追加

- 監視フォルダを既定でこの PC 内（ローカルディスク / USB）に限定する。UNC とネットワークドライブは拒否し、`settings.json` の `allowNetworkWatchFolder` で将来のネットワーク商品へ切り替えられる。管理画面からは変更しない
- ファイル名末尾 `_YYYYMMDD` による再生期限指定（指定日まで再生、秒数と順不同）。期限切れはプレイリストから除外し、日付跨ぎは次スライド境界で反映。入力誤りは再生を止めず、管理モード入場時に MessageBox（修正案内と対象ファイル名）で知らせる。6〜7 桁の末尾数字は日付の桁落ちとして誤りにする
- 壊れた JPEG / PDF / コンテナ不正の MP4 を、管理モード入場時に MessageBox（タイトル「壊れたファイル」、差し替え案内と対象ファイル名）で知らせる。再生はスキップして継続。コピー途中の排他ロックは対象外
- 設計書（`docs/設計書.md`）
- 設計判断記録（`docs/adr/`）15 件（ADR 0009: 再生期限と入力誤り、ADR 0010: 6〜7 桁の桁落ち、ADR 0011: 壊れたファイルの通知、ADR 0012: 監視フォルダをこの PC 内に限定、ADR 0013: 試用開始日は PC 側に残す、ADR 0014: ログはこの PC の LocalAppData、ADR 0015: 試用開始日の片方はマシン権限に置く）
- 課金方針と競合比較（`docs/課金方針.md`、`docs/競合比較.md`）
- 純粋ロジックのユニットテストプロジェクト（`PdfSignage.Tests`、158 件）とソリューションファイル（`SimpleDigitalSignage.sln`）
- 操作ガイドのスライド版（`docs/操作ガイド.pptx`）と生成スクリプト（`scripts/build-operation-guide-pptx.ps1`）
- オフライン課金。初回起動から暦日 60 日の試用、署名付きアクセスキー、期限切れ時は再生を止めずキオスク下端に案内。試用開始日は HKCU と LocalAppData に残す（ADR 0013）。発行は `PdfSignage.Issuer`（現場配布には含めない）
- この変更履歴

### 変更

- 試用開始日をユーザー領域（HKCU / LocalAppData）だけに置かず、マシン領域（HKLM / ProgramData）にも残す。HKLM は管理者昇格時だけ書き、失敗しても起動は続ける。システム環境変数には置かない（ADR 0015）
- 課金方針: 標準 年 20,000 円 / サイト 年 60,000 円（税抜）。買い切り・1 台 SKU・5,000 円未満は出さない。サポートは障害とライセンスに限定。申込先はキオスクの期限切れ帯に出す
- ドキュメントを `docs/` へ集約。役目を終えた要件定義書・開発計画は `docs/archive/` へ移動して凍結
- 運用ガイドに、これまで文書化されていなかった挙動を追記（スケジュール未発火の条件、設定保存後の再生位置、空フォルダ表示、設定読込失敗時のログ出力先）
- 共有フォルダのアクセス制限は導入先の共有設定に任せる方針を、運用ガイド・操作ガイドに明記
- 課金方針を更新する。試用は初回起動から暦日 60 日、画面呼称はアクセスキー、同一 PC の試用リセットは HKCU と LocalAppData の軽量残存で防ぐ（ADR 0013）。ログはローカルのみ
- 操作ガイド・運用ガイド・受け入れテスト・設計書に、試用・アクセスキー・期限切れ帯の操作と確認項目を追記
- ログ出力先を監視フォルダの親から、この PC の `%LocalAppData%\PdfSignage\logs` へ移す（ADR 0014）。共有フォルダや USB を監視してもログは PC 側に残す
- 製品名を「簡単！シンプル！サイネージ」にする。exe 名（`PdfSignage.exe`）は変えない

### 修正

- 監視フォルダのパスを末尾の `\` 付きで設定すると、`logs` フォルダが監視フォルダの親ではなく内側に作られていた問題（`PathHelper.GetLogDirectory`）

## [0.1.0] - 2026-08-28

MVP。この版より前の変更は追跡していない。

### 追加

- JPEG / PDF / MP4 の全画面ループ表示。PDF は 1 ページ = 1 スライドに展開
- ファイル名の自然順による表示順制御（`001`, `002`, `010`）
- ファイル名末尾 `_秒数` による表示時間の個別指定（5〜300 秒。動画は対象外で再生完了まで表示）
- 監視フォルダの変更検知。反映は現在のスライド完了後
- キオスク表示（枠なし全画面・タスクバー抑制・Alt+Tab 一覧からの除外）
- 管理画面（`Ctrl+Shift+M`）による全設定の GUI 操作と即時反映
- 日次スケジュールによるアプリ終了と PC 電源オフ、Windows 起動時の自動起動
- エラー時のスライドスキップ、日次ログ出力（7 日保持）、未処理例外時の自己再起動、復帰不能時のメッセージ表示
- ポータブル配布（`scripts/publish.ps1`、self-contained / win-x64）
